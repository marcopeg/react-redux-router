/**
 * TestEnvironment
 * this test does nothing at all
 * it just include the app to verify it will not trigger exceptions
 *
 * you should write your unit tests into each feature folder
 */

describe('ReactApp', function() {
    it('shoul work', function() {
        require('../../index');
    });
});
